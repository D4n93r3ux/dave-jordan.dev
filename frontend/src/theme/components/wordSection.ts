import type { StyleConfig } from '@chakra-ui/theme-tools';

const WordSection: StyleConfig = {
  baseStyle: {
    display: 'flex'
  },
  variants: {
    feelingsMetNeeds: {},
    feelingsUnmetNeeds: {},
    needs: {}
  }
};

export default WordSection;
